# Java / Maven
backend/target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
*.log
npm-debug.log*

# IDE
.idea/
*.iml
.vscode/
.DS_Store

# Env - .env.development is intentionally NOT ignored (see frontend/.env.example);
# it's a committed, non-secret local default, not a place secrets live.
# Anything ending in .local is a personal override and never gets committed.
.env
.env*.local
